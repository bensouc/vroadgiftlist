<%= turbo_frame_tag gift do%>
	<div class="card gift-card mb-4 overflow-hidden
" style='background-image: linear-gradient(to right, rgba(0, 0, 0, 0.1), rgba(0, 0, 0, 0.5)),
url("<%=gift.photo_url%>")'>
		<%= link_to  "#", class:'offer-link' do %>
			<i class="fa-solid fa-gift"></i>
		<% end %>
		<div class="card-body d-flex flex-column justify-content-between">
			<div class="card-title ">
				<h2 class=" fw-bold text-white mb-1"><%= gift.name.capitalize %>
				</h2>
				<p class="price">🪙 <%= gift.price_to_s%> €</p>
			</div>
			<div class="badges">
				<p class="badge badge-<%= gift.received? ? 'success' : 'default' %> w-fit"
        style="width: fit-content;">
					<%= gift.received? ? "🎉#{(I18n.t 'gift.purchased')}" : "🤞#{(I18n.t 'gift.not_purchased')}"%>
				</p>
				<a href="#" target="_blank" class="text-white shop-url"><%= I18n.t('application.link')%> <i class='fa fa-link'></i></a>
			</div>
		</div>
	</div>
<% end %>
