<%= turbo_frame_tag @user, class:"d-flex flex-column" do%>
	<div class="d-flex flex-row flex-wrap justify-content-center gap-3" >
		<%= render(WishlistComponent.with_collection(@wishlists,user: @user, current_user:, guest: @guest))%>
<% end %>
